{
  UIStyleBackgroundColor style=getStyle(UIStyleBackgroundColor.class);
  if (style == null) {
    style=new UIStyleBackgroundColor(color);
    style.setVisible(true);
    addStyle(style,true);
  }
 else {
    style.setColor(color);
  }
}
