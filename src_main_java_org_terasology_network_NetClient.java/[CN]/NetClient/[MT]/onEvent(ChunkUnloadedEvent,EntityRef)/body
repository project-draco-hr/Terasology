{
  if (relevantChunks.remove(event.getChunkPos())) {
    queuedInvalidatedChunkEvents.add(NetData.InvalidateChunkMessage.newBuilder().setPos(NetworkUtil.convert(event.getChunkPos())).build());
  }
}
