{
  for (int i=0; i < 6; i++) {
    if (planes[i].getA() * position.x + planes[i].getB() * position.y + planes[i].getC() * position.z + planes[i].getD() <= -radius) {
      return false;
    }
  }
  return true;
}
