{
  Server server=new Server();
  server.start(7777);
  Thread.sleep(500);
  Client client=new Client();
  client.join("localhost",7777);
  Thread.sleep(500);
  server.shutdown();
  client.disconnect();
}
