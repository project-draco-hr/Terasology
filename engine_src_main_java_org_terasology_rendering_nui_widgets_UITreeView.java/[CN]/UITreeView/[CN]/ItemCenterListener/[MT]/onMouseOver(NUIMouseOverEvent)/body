{
  super.onMouseOver(event);
  if (state.getDraggedItemIndex() != null && model.get().getItem(index).acceptsChild(model.get().getItem(state.getDraggedItemIndex()))) {
    onItemMouseOver(index,MouseOverItemType.CENTER);
  }
}
