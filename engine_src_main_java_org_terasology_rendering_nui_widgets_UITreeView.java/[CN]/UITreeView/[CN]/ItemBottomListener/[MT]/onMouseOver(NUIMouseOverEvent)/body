{
  super.onMouseOver(event);
  if (state.getDraggedItemIndex() != null && !model.get().getItem(index).isRoot() && model.get().getItem(index).getParent().acceptsChild(model.get().getItem(state.getDraggedItemIndex()))) {
    onItemMouseOver(index,MouseOverItemType.BOTTOM);
  }
}
