{
  FloatBuffer matrixBuffer=BufferUtils.createFloatBuffer(16);
  for (int i=0; i < 4; i++) {
    for (int j=0; j < 4; j++) {
      matrixBuffer.put(mat.getElement(j,i));
    }
  }
  matrixBuffer.flip();
  return matrixBuffer;
}
