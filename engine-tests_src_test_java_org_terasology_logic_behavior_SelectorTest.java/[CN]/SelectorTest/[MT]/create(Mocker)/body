{
  return new Node(){
    @Override public Task create(){
      Task spy=spy(new Task(null){
        @Override public Status update(        float dt){
          return null;
        }
      }
);
      mocker.mock(spy);
      return spy;
    }
  }
;
}
