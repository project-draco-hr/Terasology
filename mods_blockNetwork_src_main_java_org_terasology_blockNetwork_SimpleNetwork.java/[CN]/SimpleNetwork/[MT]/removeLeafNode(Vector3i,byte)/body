{
  final boolean changed=leafNodes.remove(location,connectingOnSides);
  if (!changed)   throw new IllegalStateException("Tried to remove a node that is not in the network");
  distanceCache.clear();
  if (isDegeneratedNetwork())   return true;
  return isEmptyNetwork();
}
