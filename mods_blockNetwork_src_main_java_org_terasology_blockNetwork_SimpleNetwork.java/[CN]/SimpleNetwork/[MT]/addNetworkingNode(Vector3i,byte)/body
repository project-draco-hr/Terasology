{
  if (SANITY_CHECK && !canAddNetworkingNode(location,connectingOnSides))   throw new IllegalStateException("Unable to add this node to network");
  networkingNodes.put(new Vector3i(location),connectingOnSides);
  distanceCache.clear();
}
