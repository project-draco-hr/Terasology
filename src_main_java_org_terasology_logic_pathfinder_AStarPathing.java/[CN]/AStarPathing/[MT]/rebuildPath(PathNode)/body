{
  List<Vector3f> path=new ArrayList<Vector3f>();
  PathNode node=targetNode;
  while (node != null) {
    if (node.parent == null) {
      break;
    }
    path.add(0,node.getCenterVector());
    node=node.parent;
  }
  return path;
}
