{
  if (value != null) {
    Set<T> result=Sets.newHashSetWithExpectedSize(value.size());
    for (    T item : value) {
      result.add(contentStrategy.copy(item));
    }
    return result;
  }
  return null;
}
