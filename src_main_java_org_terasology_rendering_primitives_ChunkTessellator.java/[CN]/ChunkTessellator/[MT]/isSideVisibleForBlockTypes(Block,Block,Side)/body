{
  if (currentBlock.getSideMesh(side) == null)   return false;
  if (currentBlock.isLiquid() && blockToCheck.isLiquid())   return false;
  return blockToCheck.getId() == 0x0 || !blockToCheck.isBlockingSide(side.reverse()) || (!currentBlock.isTranslucent() && blockToCheck.isTranslucent());
}
