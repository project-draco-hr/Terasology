{
  if (_lock.tryLock()) {
    if (!_disposed) {
      ChunkMesh[] oldNewMesh=_newMeshes;
      _newMeshes=newMesh;
      if (oldNewMesh != null) {
        for (int i=0; i < oldNewMesh.length; i++)         oldNewMesh[i].dispose();
      }
    }
    _lock.unlock();
  }
}
