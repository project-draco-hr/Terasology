{
  AssetData data=loadAssetData(asset.getURI(),false);
  if (data != null) {
    try {
      asset.reload((D)data);
    }
 catch (    Exception e) {
      logger.error("Could not reload asset " + asset.getURI(),e);
    }
  }
}
