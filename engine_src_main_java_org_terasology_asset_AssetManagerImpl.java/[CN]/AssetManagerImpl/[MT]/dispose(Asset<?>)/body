{
  if (!asset.isDisposed()) {
    try {
      asset.dispose();
    }
 catch (    Exception e) {
      logger.error("Could not dispose asset {}",asset.getURI(),e);
    }
  }
  assetCache.remove(asset.getURI());
}
