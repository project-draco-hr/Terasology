{
  NetData.NetMessage message=NetData.NetMessage.newBuilder().addEvent(NetData.EventMessage.newBuilder().setEvent(eventSerializer.serialize(event,new NetworkEventFieldCheck())).setTargetId(targetId)).build();
  send(message);
}
