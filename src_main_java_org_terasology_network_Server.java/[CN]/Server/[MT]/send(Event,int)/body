{
  NetData.NetMessage message=NetData.NetMessage.newBuilder().addEvent(NetData.EventMessage.newBuilder().setEvent(eventSerializer.serialize(event,FieldSerializeCheck.NullCheck.<Event>newInstance())).setTargetId(targetId)).build();
  send(message);
}
