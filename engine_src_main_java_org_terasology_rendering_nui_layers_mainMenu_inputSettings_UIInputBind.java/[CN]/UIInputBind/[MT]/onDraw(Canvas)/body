{
  if (!capturingInput) {
    if (input.get() != null) {
      canvas.drawText(input.get().getDisplayName());
    }
  }
 else {
    canvas.drawText("???");
  }
  canvas.addInteractionRegion(interactionListener);
}
