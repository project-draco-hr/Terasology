{
  Vector3f v=null;
synchronized (_pool) {
    if (_pool.size() > 0) {
      v=_pool.removeFirst();
    }
  }
  if (v == null) {
    v=new Vector3f(x,y,z);
  }
 else {
    v.set(x,y,z);
  }
  return v;
}
