{
  double diff=Math.abs(_activeFov - _targetFov);
  if (diff < 1.0) {
    _activeFov=_targetFov;
    return;
  }
  if (_activeFov < _targetFov)   _activeFov+=50 * delta;
 else   if (_activeFov > _targetFov)   _activeFov-=50 * delta;
}
