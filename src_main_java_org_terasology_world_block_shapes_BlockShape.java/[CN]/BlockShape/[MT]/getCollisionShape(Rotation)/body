{
  if (isCollisionSymmetric()) {
    return collisionShape.get(Rotation.NONE);
  }
  CollisionShape result=collisionShape.get(rot);
  if (result == null)   return new BoxShape(new Vector3f(0.5f,0.5f,0.5f));
  return result;
}
