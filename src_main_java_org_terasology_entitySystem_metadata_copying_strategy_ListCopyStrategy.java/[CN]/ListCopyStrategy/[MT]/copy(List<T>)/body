{
  if (value != null) {
    List<T> result=Lists.newArrayListWithCapacity(value.size());
    for (    T item : value) {
      result.add(contentStrategy.copy(item));
    }
    return result;
  }
  return null;
}
