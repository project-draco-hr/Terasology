{
  Block removedBlock=removeBlock(true);
  if (removedBlock != null) {
    if (removedBlock.getBlockGroup().getTitle().equals("Torch"))     _player.getInventory().addItem(new ItemBlock(removedBlock.getBlockGroup()),1);
  }
}
