{
  if (!_running)   _activeWalkingSpeed=_walkingSpeed;
 else   _activeWalkingSpeed=_walkingSpeed * _runningFactor;
  processMovement();
  updatePosition();
  checkPosition();
  updateSwimStatus();
  playMovementSound();
}
