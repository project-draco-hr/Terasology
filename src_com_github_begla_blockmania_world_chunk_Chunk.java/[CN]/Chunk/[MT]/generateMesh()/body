{
  if (!isCached() || _fresh)   return;
  if (_newMesh != null) {
    _newMesh.freeBuffers();
    _newMesh=null;
  }
  _newMesh=_meshGenerator.generateMesh();
  setDirty(false);
  _statVertexArrayUpdateCount++;
}
