{
  if (isFresh() || isLightDirty())   return;
  double distance=distanceToPlayer();
  boolean createPhysicsMesh=distance < 64.0;
  setNewMesh(_meshGenerator.generateMesh(createPhysicsMesh));
  setDirty(false);
}
