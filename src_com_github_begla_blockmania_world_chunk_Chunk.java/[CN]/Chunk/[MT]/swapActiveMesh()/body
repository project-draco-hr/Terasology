{
synchronized (this) {
    if (_disposed)     return;
    if (_newMesh != null) {
      if (!_newMesh.isGenerated() || _newMesh.isDisposed())       return;
      ChunkMesh newMesh=_newMesh;
      _newMesh=null;
      ChunkMesh oldActiveMesh=_activeMesh;
      _activeMesh=newMesh;
      if (oldActiveMesh != null)       oldActiveMesh.dispose();
    }
  }
}
