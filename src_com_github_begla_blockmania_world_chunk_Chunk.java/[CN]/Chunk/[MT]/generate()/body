{
  if (isFresh()) {
    setFresh(false);
    for (    ChunkGenerator gen : _parent.getGeneratorManager().getChunkGenerators()) {
      gen.generate(this);
    }
    generateSunlight();
    return true;
  }
  return false;
}
