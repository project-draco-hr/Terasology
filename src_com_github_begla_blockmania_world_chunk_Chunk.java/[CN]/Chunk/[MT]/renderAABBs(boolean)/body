{
  if (isReadyForRendering()) {
    for (int i=0; i < VERTICAL_SEGMENTS; i++) {
      if (!isSubMeshEmpty(i)) {
        if (!solid)         getSubMeshAABB(i).render(2f);
 else         getSubMeshAABB(i).renderSolid();
      }
    }
  }
}
