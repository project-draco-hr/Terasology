{
  if (_newMesh != null) {
    for (    Chunk nc : loadOrCreateNeighbors())     if ((nc.isDirty() || nc.isLightDirty()) && nc.isVisible())     return;
    if (_newMesh.isGenerated() && !isDirty() && !isFresh()&& !isLightDirty()) {
      _activeMesh=_newMesh;
      _newMesh=null;
    }
  }
}
