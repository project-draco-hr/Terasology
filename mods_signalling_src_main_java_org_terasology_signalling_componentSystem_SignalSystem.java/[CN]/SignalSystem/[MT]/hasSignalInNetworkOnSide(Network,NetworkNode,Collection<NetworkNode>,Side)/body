{
  for (  NetworkNode producerLocation : producers) {
    final int signalStrength=producerSignalStrengths.get(producerLocation);
    if (network.isInDistanceWithSide(signalStrength,producerLocation,consumerNode,sideInNetwork))     return true;
  }
  return false;
}
