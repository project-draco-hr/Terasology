{
  if ((factor > 0 && angle > currentAngle) || (factor < 0 && angle < currentAngle)) {
    currentAngle+=factor * speed;
  }
 else {
    if (!isRepeat()) {
      currentAngle=angle;
      this.stop();
    }
 else {
      currentAngle=0;
    }
  }
}
