{
  return (Collection.class.isAssignableFrom(type) && ReflectionUtil.getTypeParameter(genericType,0).equals(targetType)) || (Map.class.isAssignableFrom(type) && ReflectionUtil.getTypeParameter(genericType,1).equals(targetType));
}
