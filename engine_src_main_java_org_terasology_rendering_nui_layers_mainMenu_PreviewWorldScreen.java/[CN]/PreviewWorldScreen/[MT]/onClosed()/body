{
  super.onClosed();
  if (environment != null) {
    CoreRegistry.get(AssetManager.class).setEnvironment(moduleManager.getEnvironment());
    environment.close();
    environment=null;
  }
  if (previewGen != null) {
    previewGen.close();
    previewGen=null;
  }
}
