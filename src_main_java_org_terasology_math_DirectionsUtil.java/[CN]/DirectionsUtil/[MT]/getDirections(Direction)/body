{
  byte result=0;
  for (  Direction direction : directions) {
    final byte directionBit=directionBits.get(direction);
    if ((result & directionBit) > 0)     throw new IllegalArgumentException("Cannot have multiples of the same direction");
    result+=directionBit;
  }
  return result;
}
