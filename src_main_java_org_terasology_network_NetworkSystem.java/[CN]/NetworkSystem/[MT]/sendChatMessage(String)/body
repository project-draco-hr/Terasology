{
  NetData.NetMessage data=NetData.NetMessage.newBuilder().setType(NetData.NetMessage.Type.CONSOLE).setConsole(NetData.ConsoleMessage.newBuilder().setMessage(message)).build();
  allChannels.write(data);
}
