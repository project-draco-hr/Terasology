{
  final List<RenderableNode> result=Lists.newArrayList();
  root.visit(null,new Node.Visitor<RenderableNode>(){
    @Override public RenderableNode visit(    RenderableNode item,    Node node){
      result.add(renderableNodes.get(node));
      return null;
    }
  }
);
  return result;
}
