{
  Runnable r=new Runnable(){
    public void run(){
      for (      Chunk c : _chunkCache.values()) {
        writeChunkToDisk(c);
        c.dispose();
      }
      _chunkCache.clear();
    }
  }
;
  Blockmania.getInstance().getThreadPool().submit(r);
}
