{
  if (chunks.size() == 0) {
    return;
  }
  final Iterator<WeakReference<ChunkImpl>> it=chunks.iterator();
  while (it.hasNext()) {
    final WeakReference<ChunkImpl> w=it.next();
    if (w.get() == null) {
      it.remove();
    }
  }
}
