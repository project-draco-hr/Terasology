{
  if (_states.size() > 0) {
    if (stateID < 0)     stateID=0;
    if (stateID >= _states.size())     stateID=_states.size() - 1;
    _states.remove(stateID);
    if (_states.size() == 0) {
      _currentState=-1;
      getLabel().setText("");
    }
 else {
      nextState();
    }
  }
}
