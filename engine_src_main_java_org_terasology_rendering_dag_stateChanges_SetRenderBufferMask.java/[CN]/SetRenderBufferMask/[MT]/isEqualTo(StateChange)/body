{
  if (stateChange instanceof SetRenderBufferMask) {
    SetRenderBufferMask change=(SetRenderBufferMask)stateChange;
    return change.fboName.equals(this.fboName) && change.color == this.color && change.normal == this.normal && change.lightBuffer == this.lightBuffer;
  }
  return false;
}
