{
  return new AssetFactory<StreamingSoundData,StreamingSound>(){
    @Override public StreamingSound buildAsset(    AssetUri uri,    StreamingSoundData data){
      return new NullStreamingSound(uri,data);
    }
  }
;
}
