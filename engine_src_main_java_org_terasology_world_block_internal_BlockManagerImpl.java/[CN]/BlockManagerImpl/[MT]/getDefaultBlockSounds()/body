{
  BlockSounds sounds=getBlockSounds(BlockSounds.DEFAULT_ID);
  if (sounds == null) {
    throw new IllegalStateException("Default block sounds are missing from engine module: " + BlockSounds.DEFAULT_ID);
  }
  return sounds;
}
