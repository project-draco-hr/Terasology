{
  Set<BlockTile> tileSet=Sets.newHashSet((getData().getBaseSection().getBlockTiles().values()));
  return getData().getBaseSection().getShape() == null && getData().getFamilyFactory() == null && !getData().getBaseSection().isLiquid() && tileSet.size() == 1;
}
