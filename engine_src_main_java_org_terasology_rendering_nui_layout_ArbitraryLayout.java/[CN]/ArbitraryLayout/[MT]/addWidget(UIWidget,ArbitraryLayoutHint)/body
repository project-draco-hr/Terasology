{
  if (hint != null) {
switch (hint.getMode()) {
case FILL:
      addFillWidget(element,hint.getRegion());
    break;
default :
  addFixedWidget(element,hint.getSize(),hint.getCenter());
break;
}
}
}
