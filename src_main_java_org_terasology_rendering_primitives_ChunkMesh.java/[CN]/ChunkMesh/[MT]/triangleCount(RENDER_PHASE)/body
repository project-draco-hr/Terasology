{
  if (phase == RENDER_PHASE.OPAQUE)   return _vertexCount[0] / 3;
 else   if (phase == RENDER_PHASE.BILLBOARD_AND_TRANSLUCENT)   return (_vertexCount[1] + _vertexCount[2]) / 3;
 else   return _vertexCount[3] / 3;
}
