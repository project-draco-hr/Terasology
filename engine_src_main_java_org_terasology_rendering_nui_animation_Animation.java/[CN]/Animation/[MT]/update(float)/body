{
switch (this.currentState) {
case RUNNING:
{
      if (this.frames.size() == 0) {
        end();
        return;
      }
      frames.get(0).update(delta);
      if (frames.get(0).isFinished()) {
        frames.remove(0);
        return;
      }
      break;
    }
default :
  break;
}
}
