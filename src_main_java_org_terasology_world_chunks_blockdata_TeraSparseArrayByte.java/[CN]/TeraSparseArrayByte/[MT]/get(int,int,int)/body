{
  if (!contains(x,y,z))   throw new IndexOutOfBoundsException("Index out of bounds (" + x + ", "+ y+ ", "+ z+ ")");
  if (inflated == null)   return rowGet(x,z,fill);
  byte[] row=inflated[y];
  if (row != null)   return rowGet(row,x,z);
  return rowGet(x,z,deflated[y]);
}
