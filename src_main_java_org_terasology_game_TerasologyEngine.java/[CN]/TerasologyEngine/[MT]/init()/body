{
  if (initialised)   return;
  initLogger();
  logger.log(Level.INFO,"Initializing Terasology...");
  registerUrlHandlers();
  initNativeLibs();
  initDisplay();
  initOpenGL();
  initOpenAL();
  initControls();
  initManagers();
  initTimer();
  initialised=true;
}
