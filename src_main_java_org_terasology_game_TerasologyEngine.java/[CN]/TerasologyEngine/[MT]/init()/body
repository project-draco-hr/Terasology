{
  if (initialised)   return;
  logger.log(Level.INFO,"Initializing Terasology...");
  initNativeLibs();
  initDisplay();
  initOpenGL();
  initOpenAL();
  initControls();
  initManagers();
  initTimer();
  initialised=true;
}
