{
  loadPresets();
  loadLastSettings();
  try {
    trySetWorldSetting();
    trySetUserSetting();
  }
 catch (  RuntimeException e) {
    Terasology.getInstance().getLogger().log(Level.SEVERE,e.toString(),e);
  }
}
