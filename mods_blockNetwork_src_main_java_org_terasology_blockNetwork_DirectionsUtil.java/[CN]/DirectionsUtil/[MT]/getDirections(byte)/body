{
  Set<Direction> result=Sets.newHashSet();
  for (  Map.Entry<Direction,Byte> aDirectionBit : directionBits.entrySet()) {
    if ((aDirectionBit.getValue() & directionBit) > 0)     result.add(aDirectionBit.getKey());
  }
  return result;
}
