{
  for (FastMap.Entry<Integer,Chunk> e=_chunkCache.head(), end=_chunkCache.tail(); (e=e.getNext()) != end; ) {
    Chunk c=e.getValue();
    c.setCached(false);
    writeChunkToDisk(c);
    c.freeBuffers();
  }
  _chunkCache.clear();
}
