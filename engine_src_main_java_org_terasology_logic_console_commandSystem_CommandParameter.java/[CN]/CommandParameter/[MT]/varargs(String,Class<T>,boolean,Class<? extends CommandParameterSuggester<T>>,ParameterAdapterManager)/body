{
  try {
    return varargs(name,childType,required,suggester != null ? suggester.newInstance() : null,parameterAdapterManager);
  }
 catch (  InstantiationException|IllegalAccessException e) {
    throw new SuggesterInstantiationException(e);
  }
}
