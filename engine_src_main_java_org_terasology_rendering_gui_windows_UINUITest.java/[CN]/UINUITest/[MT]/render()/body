{
  checkGLError();
  canvas.preRender();
  try (SubRegion ignored=canvas.subRegion(Rect2i.createFromMinAndSize(0,0,120,80),true)){
    button.draw(canvas);
  }
   canvas.postRender();
  checkGLError();
}
