def draw(self, context):
    layout = self.layout
    ob = context.object
    if (not ob):
        return
    if (not (ob.type == 'MESH')):
        return
    layout.prop(ob, 'teraFullSide')
    layout.prop(ob, 'teraColliderType')
