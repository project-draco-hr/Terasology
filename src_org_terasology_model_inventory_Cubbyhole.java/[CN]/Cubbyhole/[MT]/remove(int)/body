{
  if (count > _count) {
    throw new IllegalArgumentException("Removing too many items results in negative item count: " + count);
  }
  _count-=count;
  if (isEmpty()) {
    Item result=_item;
    _item=null;
    return result;
  }
  return null;
}
