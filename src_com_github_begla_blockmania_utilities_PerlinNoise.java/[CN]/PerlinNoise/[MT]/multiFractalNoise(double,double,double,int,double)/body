{
  double result=0;
  for (int i=1; i <= octaves; i++) {
    result+=noise(x,y,z) * Math.pow(lacunarity,-0.96471 * i);
    x*=lacunarity;
    y*=lacunarity;
    z*=lacunarity;
  }
  return result;
}
