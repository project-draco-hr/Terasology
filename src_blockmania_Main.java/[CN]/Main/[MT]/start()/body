{
  getDelta();
  lastFPS=getTime();
  while (!Display.isCloseRequested()) {
    int delta=getDelta();
    updateFPS();
    processKeyboard();
    processMouse();
    update(delta);
    render();
    Display.update();
    Display.sync(60);
  }
  Display.destroy();
}
