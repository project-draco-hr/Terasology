{
  if (isConsumingInput()) {
    processMouseInput(-1,false,0);
    if (renderer.getWindowFocused() != null) {
      if (renderer.getWindowFocused().isModal()) {
        event.consume();
      }
    }
  }
}
