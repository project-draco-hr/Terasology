{
  if (isConsumingInput()) {
    processMouseInput(-1,false,event.getWheelTurns() * 120);
    if (_focusedWindow != null) {
      if (_focusedWindow.isModal()) {
        event.consume();
      }
    }
  }
}
