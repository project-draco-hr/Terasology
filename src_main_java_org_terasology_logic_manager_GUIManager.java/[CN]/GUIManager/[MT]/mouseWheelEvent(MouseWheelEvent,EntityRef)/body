{
  if (isConsumingInput()) {
    processMouseInput(-1,false,event.getWheelTurns() * 120);
    if (_currentFocused != null) {
      if (_currentFocused.isModal()) {
        event.consume();
      }
    }
  }
}
