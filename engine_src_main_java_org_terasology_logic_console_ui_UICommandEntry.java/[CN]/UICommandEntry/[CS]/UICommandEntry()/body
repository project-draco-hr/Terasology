{
  subscribe(new CursorUpdateEventListener(){
    @Override public void onCursorUpdated(    int oldPosition,    int newPosition){
      if (tabCompletionEngine == null)       return;
      tabCompletionEngine.reset();
    }
  }
);
}
