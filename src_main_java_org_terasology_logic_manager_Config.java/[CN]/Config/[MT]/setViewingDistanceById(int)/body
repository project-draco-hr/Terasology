{
  _setting.getSystemBuilder().setActiveViewingDistanceId(viewingDistance);
  WorldRenderer worldRenderer=CoreRegistry.get(WorldRenderer.class);
  if (worldRenderer != null)   worldRenderer.updateChunksInProximity(true);
}
