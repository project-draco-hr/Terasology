{
  if (netRelevant.contains(targetId)) {
    queuedOutgoingEvents.add(NetData.EventMessage.newBuilder().setTargetId(targetId).setEvent(eventSerializer.serialize(event,EVENT_FIELD_CHECK)).build());
  }
}
