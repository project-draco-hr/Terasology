{
  disposed=true;
  if (rigidBody != null) {
    rigidBody.destroy();
    rigidBody=null;
  }
  if (mesh != null) {
    for (    ChunkMesh chunkMesh : mesh) {
      chunkMesh.dispose();
    }
    mesh=null;
  }
}
