{
  if (focus) {
    for (    FocusListener listener : _focusListeners) {
      listener.focusOn(this);
    }
  }
 else {
    for (    FocusListener listener : _focusListeners) {
      listener.focusOff(this);
    }
  }
}
