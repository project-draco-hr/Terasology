{
  if (focus) {
    for (    IFocusListener listener : _focusListeners) {
      listener.focusOn(this);
    }
  }
 else {
    for (    IFocusListener listener : _focusListeners) {
      listener.focusOff(this);
    }
  }
}
