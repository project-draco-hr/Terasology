{
  @SuppressWarnings("unchecked") ArrayList<VisibilityListener> listeners=(ArrayList<VisibilityListener>)visibilityListeners.clone();
  for (  VisibilityListener listener : listeners) {
    listener.changed(this,visible);
  }
}
