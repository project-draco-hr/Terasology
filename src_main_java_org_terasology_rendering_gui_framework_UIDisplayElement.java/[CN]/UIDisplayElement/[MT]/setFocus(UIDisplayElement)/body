{
  if (_focusedElement != focus) {
    if (focus == null && _focusedElement != this)     return;
    if (_focusedElement != null)     _focusedElement.notifyFocusListeners(false);
    _focusedElement=focus;
    if (_focusedElement != null)     _focusedElement.notifyFocusListeners(true);
  }
}
