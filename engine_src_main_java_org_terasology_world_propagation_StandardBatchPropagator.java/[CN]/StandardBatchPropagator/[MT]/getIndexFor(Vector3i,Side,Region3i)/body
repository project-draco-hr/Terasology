{
switch (side) {
case TOP:
case BOTTOM:
    return pos.x + region.size().x * pos.z;
case LEFT:
case RIGHT:
  return pos.y + region.size().y * pos.z;
default :
return pos.x + region.size().x * pos.y;
}
}
