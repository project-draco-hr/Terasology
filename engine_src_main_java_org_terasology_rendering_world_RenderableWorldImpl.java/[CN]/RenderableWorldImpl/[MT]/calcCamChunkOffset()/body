{
  return new Vector3i((int)(activeCamera.getPosition().x / ChunkConstants.SIZE_X),(int)(activeCamera.getPosition().y / ChunkConstants.SIZE_Y),(int)(activeCamera.getPosition().z / ChunkConstants.SIZE_Z));
}
