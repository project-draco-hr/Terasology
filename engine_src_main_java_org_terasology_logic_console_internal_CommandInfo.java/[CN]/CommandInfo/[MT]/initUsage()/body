{
  String name=getName();
  StringBuilder builder=new StringBuilder(name);
  for (  CommandParameterInfo param : getParameters())   builder.append(' ').append(param.getUsage());
  return lazyUsage=builder.toString();
}
