{
  if (!fade) {
    return;
  }
  float delta=(srcGain - targetGain) / 100;
  setGain(getGain() - delta);
  if (getGain() >= targetGain) {
    if (targetGain == 0.0f) {
      stop();
    }
    fade=false;
  }
}
