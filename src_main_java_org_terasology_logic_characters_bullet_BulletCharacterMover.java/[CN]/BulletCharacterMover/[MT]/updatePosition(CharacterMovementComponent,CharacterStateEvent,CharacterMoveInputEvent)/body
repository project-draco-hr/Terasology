{
switch (state.getMode()) {
case GHOSTING:
    ghost(movementComp,state,input);
  break;
case SWIMMING:
swim(movementComp,state,input);
break;
case WALKING:
walk(movementComp,state,input);
break;
default :
walk(movementComp,state,input);
break;
}
}
