{
  double result=0.0;
  double workingX=x;
  double workingY=y;
  double workingZ=z;
  for (int i=0; i < octaves; i++) {
    result+=other.noise(workingX,workingY,workingZ) * spectralWeights[i];
    workingX*=LACUNARITY;
    workingY*=LACUNARITY;
    workingZ*=LACUNARITY;
  }
  return result;
}
