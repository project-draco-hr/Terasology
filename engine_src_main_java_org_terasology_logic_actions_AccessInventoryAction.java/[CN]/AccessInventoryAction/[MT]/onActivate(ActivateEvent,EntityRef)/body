{
  EntityRef instigator=event.getInstigator();
  if (instigator.getComponent(CharacterComponent.class) != null && instigator.getComponent(InventoryComponent.class) != null) {
    CharacterUtil.setInteractionTarget(instigator,entity);
  }
}
