{
  return TeraMath.calcChunkPosX(blockPos.x,ChunkConstants.POWER_X) - topLeft.x + 3 * (TeraMath.calcChunkPosY(blockPos.y,ChunkConstants.POWER_Y) - topLeft.y + 3 * (TeraMath.calcChunkPosZ(blockPos.z,ChunkConstants.POWER_Z) - topLeft.z));
}
