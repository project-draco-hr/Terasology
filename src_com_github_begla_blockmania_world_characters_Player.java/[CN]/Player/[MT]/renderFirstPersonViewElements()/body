{
  Tool activeTool=_toolBelt.getSelectedTool();
  if (activeTool == null)   renderHand();
 else   if (activeTool != null && !BlockPlacementRemovalTool.class.isInstance(activeTool))   renderHand();
 else   renderSelectedBlock();
}
