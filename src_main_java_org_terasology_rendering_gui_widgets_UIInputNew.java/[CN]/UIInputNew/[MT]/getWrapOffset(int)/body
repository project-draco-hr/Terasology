{
  for (int i=0; i < wrapPosition.size(); i++) {
    if (wrapPosition.get(i) >= index) {
      return index - i;
    }
 else     if (i == wrapPosition.size() - 1) {
      return index - i - 1;
    }
  }
  return index;
}
