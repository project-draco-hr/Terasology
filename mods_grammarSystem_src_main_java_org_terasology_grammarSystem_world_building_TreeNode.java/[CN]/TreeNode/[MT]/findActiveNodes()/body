{
  List<TreeNode> retVal=new ArrayList<TreeNode>();
  if (active) {
    retVal.add(this);
  }
  for (  Tree t : children) {
    if (t instanceof TreeNode) {
      retVal.addAll(t.findActiveNodes());
    }
  }
  return retVal;
}
