{
  eventSystem.process();
  for (  ComponentSystem system : componentSystemManager.iterateAll()) {
    system.shutdown();
  }
  stopBackgroundMusic();
  GUIManager.getInstance().closeAllWindows();
  entityManager.clear();
}
