{
  eventSystem.process();
  for (  ComponentSystem system : componentSystemManager.iterateAll()) {
    system.shutdown();
  }
  stopBackgroundMusic();
  GUIManager.getInstance().removeAllWindows();
  entityManager.clear();
}
