{
  try {
    int bogus=0;
    for (int i=0; i < repetitions; i++) {
      (new ObjectOutputStream(out)).writeObject(array);
      bogus+=out.size();
      out.reset();
    }
    return bogus;
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
