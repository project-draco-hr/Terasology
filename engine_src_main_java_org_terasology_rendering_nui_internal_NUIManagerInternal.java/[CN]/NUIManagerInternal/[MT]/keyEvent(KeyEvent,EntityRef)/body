{
  if (focus != null) {
    focus.onKeyEvent(new NUIKeyEvent(mouse,keyboard,event.getKey(),event.getKeyCharacter(),event.getState()));
  }
  if (event.isDown() && !event.isConsumed() && event.getKey() == Keyboard.Key.ESCAPE) {
    for (    UIScreenLayer screen : screens) {
      if (screen.isEscapeToCloseAllowed()) {
        closeScreen(screen);
        event.consume();
        break;
      }
 else       if (screen.isModal()) {
        break;
      }
    }
  }
  for (  UIScreenLayer screen : screens) {
    if (screen.isModal()) {
      return;
    }
  }
}
