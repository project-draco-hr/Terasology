{
  Side frontResult=rotation.rotate(Side.FRONT);
  Side topResult=rotation.rotate(Side.TOP);
  byte[] result=new byte[]{127};
  NORMALIZED_ROTATIONS.forEachEntry(new TByteObjectProcedure<Rotation>(){
    @Override public boolean execute(    byte a,    Rotation b){
      if (b.rotate(Side.FRONT) == frontResult && b.rotate(Side.TOP) == topResult) {
        result[0]=a;
        return false;
      }
      return true;
    }
  }
);
  if (result[0] != 127) {
    return result[0];
  }
 else {
    return null;
  }
}
