{
  boolean result=true;
  for (  UIDisplayElement screen : _renderer.getDisplayElements()) {
    if (screen.isVisible() && !screen.isOverlay() && screen != s)     result=false;
  }
  return result;
}
