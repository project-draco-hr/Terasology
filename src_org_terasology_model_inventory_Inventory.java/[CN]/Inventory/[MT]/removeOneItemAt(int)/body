{
  if (slot < 0 || slot >= size())   return null;
  if (_items[slot] != null) {
    Item item=_items[slot];
    _counts[slot]--;
    if (_counts[slot] == 0) {
      _items[slot]=null;
    }
    return item;
  }
  return null;
}
