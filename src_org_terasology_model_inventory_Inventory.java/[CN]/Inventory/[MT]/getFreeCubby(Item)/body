{
  for (  Cubbyhole cubby : _cubbies) {
    if (cubby.getItem() == null) {
      return cubby;
    }
    if (cubby.getItem().equals(item) && !cubby.isFull()) {
      return cubby;
    }
  }
  return null;
}
