{
  for (int i=0; i < size(); i++) {
    if (_items[i] == null) {
      return i;
    }
 else {
      if (_items[i].equals(item) && _counts[i] < item.getStackSize()) {
        return i;
      }
    }
  }
  return -1;
}
