{
  Color f=(Color)theFrom;
  Color t=(Color)theTo;
  setValue(new Color(clamp(v * (t.r() - f.r()) + f.r()),clamp(v * (t.g() - f.g()) + f.g()),clamp(v * (t.b() - f.b()) + f.b()),clamp(v * (t.a() - f.a()) + f.a())));
}
