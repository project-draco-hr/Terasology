{
  float river=(float)TeraMath.clamp((java.lang.Math.sqrt(java.lang.Math.abs(riverNoise.noise(0.0008f * x,0.0008f * z))) - 0.1) * 7.0);
  return baseProvider.getHeightAt(x,z) * TeraMath.clamp(river + 0.25f);
}
