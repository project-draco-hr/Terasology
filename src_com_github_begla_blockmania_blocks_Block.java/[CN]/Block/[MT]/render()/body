{
  if (isBlockInvisible())   return;
  if (_displayList == -1)   generateDisplayList();
  glEnable(GL_TEXTURE_2D);
  TextureManager.getInstance().bindTexture("terrain");
  glCallList(_displayList);
  glDisable(GL11.GL_TEXTURE_2D);
}
