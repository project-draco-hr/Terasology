{
  return new Vector4f((float)Integer.parseInt(checkHex(hexColor).substring(0,2),16),(float)Integer.parseInt(checkHex(hexColor).substring(2,4),16),(float)Integer.parseInt(checkHex(hexColor).substring(4,6),16),1.0f);
}
