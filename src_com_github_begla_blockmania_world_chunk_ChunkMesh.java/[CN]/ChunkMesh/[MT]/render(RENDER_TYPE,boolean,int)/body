{
switch (type) {
case OPAQUE:
    renderVbo(side);
  break;
case BILLBOARD_AND_TRANSLUCENT:
renderVbo(7);
if (!distantChunk) {
glDisable(GL_CULL_FACE);
renderVbo(6);
glEnable(GL_CULL_FACE);
}
break;
case WATER_AND_ICE:
renderVbo(8);
break;
}
}
