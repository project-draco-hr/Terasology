{
  ComponentMetadata<InventoryComponent> classMetadata=new ComponentMetadata<>(new SimpleUri("unittest:inventory"),InventoryComponent.class,factory,copyStrategyLibrary);
  ComponentFieldMetadata metadata=classMetadata.getField("itemSlots");
  assertTrue(metadata.isOwnedReference());
}
