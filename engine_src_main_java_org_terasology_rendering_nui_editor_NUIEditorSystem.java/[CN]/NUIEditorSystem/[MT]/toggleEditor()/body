{
  if (editorActive) {
    if (screenSelected) {
      nuiManager.popScreen();
    }
    nuiManager.popScreen();
  }
 else {
    if (screenSelected) {
      pushSelectedScreen();
    }
    nuiManager.pushScreen(NUI_EDITOR_URN);
  }
  editorActive=!editorActive;
}
