{
  if (focus) {
    for (    FocusListener listener : focusListeners) {
      listener.focusOn(this);
    }
  }
 else {
    for (    FocusListener listener : focusListeners) {
      listener.focusOff(this);
    }
  }
}
