{
  @SuppressWarnings("unchecked") List<VisibilityListener> listeners=Lists.newArrayList(visibilityListeners);
  for (  VisibilityListener listener : listeners) {
    listener.changed(this,visible);
  }
}
