{
  RayBlockIntersection.Intersection is=_player.calcSelectedBlock();
  if (is == null)   return;
  if (_currentBlueprint == null)   addBlock(is.getBlockPosition());
 else   _currentBlueprint.build(_player.getParent().getWorldProvider(),is.getBlockPosition());
}
