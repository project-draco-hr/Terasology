{
  ArbritaryDelayActionComponent a1=new ArbritaryDelayActionComponent();
  a1.value=3;
  vals=new ArrayList<Integer>(Arrays.asList(3));
  long currentTime=time.getGameTimeInMs();
  delayedActionSystem.addDelayedAction(createFakeEntityWith(a1),"Blah Blah",((currentTime + 1000) - currentTime));
  nextFakeEntityId++;
}
