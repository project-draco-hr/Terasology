{
  for (int i=_worldTimeEvents.size() - 1; i >= 0; i--) {
    final WorldTimeEvent event=_worldTimeEvents.get(i);
    event.Execute();
    if (!event.repeatingEvent)     _worldTimeEvents.remove(i);
  }
}
