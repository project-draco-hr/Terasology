{
  for (int i=0; i < vn.length + 1; i++) {
    addIndices(vertexCount,vertexCount + i + 1,vertexCount + i + 2);
  }
  addVertex(v1);
  addVertex(v2);
  addVertex(v3);
  for (  Vector3f v : vn) {
    addVertex(v);
  }
  return this;
}
