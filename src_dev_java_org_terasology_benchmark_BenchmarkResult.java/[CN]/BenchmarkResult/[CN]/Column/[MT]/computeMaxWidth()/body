{
  if (!hasMaxWidth) {
    int max=name.length();
    for (int i=0; i < reps; i++) {
      String v=getValue(i);
      if (v.length() > max) {
        max=v.length();
      }
    }
    hasMaxWidth=true;
    maxWidth=max;
  }
  return maxWidth;
}
