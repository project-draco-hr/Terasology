{
  if (o == null) {
    return 0;
  }
  double distance=distanceToCamera();
  double distance2=o.distanceToCamera();
  if (distance == distance2)   return 0;
  return distance2 > distance ? -1 : 1;
}
