{
  generate();
  Chunk[] neighbors=loadOrCreateNeighbors();
  for (  Chunk neighbor : neighbors) {
    if (neighbor != null) {
      neighbor.generate();
    }
  }
  updateLight();
  generateMeshes();
}
