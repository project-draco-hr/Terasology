{
  if (getTime() > 0.6) {
    _skysphere.render();
  }
 else {
    renderStars();
  }
  _player.applyPlayerModelViewMatrix();
  _player.render();
  renderChunks();
  _clouds.render();
  _blockParticleEmitter.render();
}
