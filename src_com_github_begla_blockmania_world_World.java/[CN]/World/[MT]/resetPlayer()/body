{
  if (_spawningPoint == null) {
    _spawningPoint=findSpawningPoint();
    _player.resetEntity();
    _player.setPosition(_spawningPoint);
  }
 else {
    _player.resetEntity();
    _player.setPosition(_spawningPoint);
  }
}
