{
  for (  EntityRef entityRef : entityCache.values()) {
    ((PojoEntityRef)entityRef).invalidate();
  }
  store.clear();
  nextEntityId=1;
  loadedIds.clear();
  freedIds.clear();
  entityCache.clear();
}
