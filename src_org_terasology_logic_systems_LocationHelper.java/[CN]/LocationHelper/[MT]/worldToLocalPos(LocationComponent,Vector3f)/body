{
  if (location.parent == null)   return position;
  LocationComponent parent=location.parent.getComponent(LocationComponent.class);
  if (parent != null) {
    worldToLocalPosTransform(parent,new Vector3f(position));
  }
  return position;
}
