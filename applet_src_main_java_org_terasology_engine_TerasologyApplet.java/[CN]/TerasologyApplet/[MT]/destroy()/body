{
  if (engine != null) {
    engine.shutdown();
  }
  try {
    gameThread.join();
  }
 catch (  InterruptedException e) {
    if (logger != null) {
      logger.error("Failed to cleanly shut down engine",e);
    }
  }
  super.destroy();
}
