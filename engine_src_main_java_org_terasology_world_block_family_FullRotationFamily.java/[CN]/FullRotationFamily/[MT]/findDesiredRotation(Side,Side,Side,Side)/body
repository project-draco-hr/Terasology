{
  for (  Rotation rotation : Rotation.values()) {
    if (rotation.rotate(originalSide) == relativeSide && rotation.rotate(originalLeftSide) == resultSide) {
      return rotation;
    }
  }
  return null;
}
