{
  if (!_running)   _activeWalkingSpeed=_walkingSpeed;
 else   _activeWalkingSpeed=_walkingSpeed * _runningFactor;
  setViewingDirection(_yaw,_pitch);
  processMovement();
  updatePosition();
  checkPosition();
  updateSwimStatus();
  _movementDirection.set(0,0,0);
  playMovementSound();
}
