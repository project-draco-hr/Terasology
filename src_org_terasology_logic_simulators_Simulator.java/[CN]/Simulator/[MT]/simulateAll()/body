{
  if (_running)   return;
  _running=true;
  Runnable r=new Runnable(){
    public void run(){
      while (executeSimulation())       ;
      _running=false;
    }
  }
;
  Terasology.getInstance().submitTask(_name + "Complete",r);
}
