{
  ModuleManager moduleManager=CoreRegistry.get(ModuleManager.class);
  new EntitySystemBuilder().build(moduleManager.getEnvironment(),CoreRegistry.get(NetworkSystem.class),CoreRegistry.get(ReflectFactory.class),CoreRegistry.get(CopyStrategyLibrary.class));
  return true;
}
