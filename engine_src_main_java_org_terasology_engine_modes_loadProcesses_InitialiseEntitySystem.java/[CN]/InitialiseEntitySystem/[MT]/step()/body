{
  ModuleManager moduleManager=CoreRegistry.get(ModuleManager.class);
  new EntitySystemBuilder().build(moduleManager,CoreRegistry.get(NetworkSystem.class),CoreRegistry.get(ReflectFactory.class),CoreRegistry.get(CopyStrategyLibrary.class));
  return true;
}
