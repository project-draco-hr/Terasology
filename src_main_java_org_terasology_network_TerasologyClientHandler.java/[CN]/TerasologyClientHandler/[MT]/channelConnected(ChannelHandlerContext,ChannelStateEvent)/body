{
  e.getChannel().write(NetMessage.newBuilder().setType(NetMessage.Type.CLIENT_CONNECT).setClientConnect(ClientConnectMessage.newBuilder().setName(CoreRegistry.get(Config.class).getPlayerConfig().getName())).build());
}
