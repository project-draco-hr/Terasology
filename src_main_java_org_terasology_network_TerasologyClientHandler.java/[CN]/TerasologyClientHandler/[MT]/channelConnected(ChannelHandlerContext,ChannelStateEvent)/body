{
  this.server=new Server(networkSystem,e.getChannel());
  networkSystem.setServer(server);
  e.getChannel().write(NetMessage.newBuilder().setClientConnect(ClientConnectMessage.newBuilder().setName(CoreRegistry.get(Config.class).getPlayerConfig().getName())).build());
}
