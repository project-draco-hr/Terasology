{
  if (task == null) {
    if (isTheDefaultInstance()) {
      fbm=dynamicFBOsManager;
    }
    task=new SetViewportSizeOfTask(resourceUrn);
    fbm.subscribe(this);
    update();
  }
  return task;
}
