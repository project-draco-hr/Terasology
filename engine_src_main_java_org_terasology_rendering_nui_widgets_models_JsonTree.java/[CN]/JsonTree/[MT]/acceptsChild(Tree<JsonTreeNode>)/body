{
  if (child == null) {
    return false;
  }
  if (getValue().getType() == JsonTreeNode.ElementType.PRIMITIVE || getValue().getType() == JsonTreeNode.ElementType.NULL) {
    return false;
  }
  if (getValue().getType() == JsonTreeNode.ElementType.ARRAY && child.getValue().getType() == JsonTreeNode.ElementType.PRIMITIVE) {
    return false;
  }
  return true;
}
