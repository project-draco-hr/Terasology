{
  if (isAlive()) {
    glPushMatrix();
    glTranslatef(_position.x,_position.y,_position.z);
    applyOrientation();
    if (_displayList == -1) {
      glGenLists(_displayList);
      renderParticle();
      glEndList();
    }
    glCallList(_displayList);
    glPopMatrix();
  }
}
