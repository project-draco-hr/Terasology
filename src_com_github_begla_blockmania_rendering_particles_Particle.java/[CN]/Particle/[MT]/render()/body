{
  if (isAlive()) {
    glPushMatrix();
    glTranslatef(_position.x,_position.y,_position.z);
    applyOrientation();
    glScalef(_size,_size,_size);
    renderParticle();
    glPopMatrix();
  }
}
