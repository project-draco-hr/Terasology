{
  if (reader != null) {
    dispose();
  }
  try {
    reader=new OggReader(url.openStream());
  }
 catch (  IOException e) {
    throw new RuntimeException("Failed to reset ogg stream from " + url,e);
  }
}
