{
  if (reader != null) {
    dispose();
  }
  try {
    reader=new OggReader(stream.openStream());
  }
 catch (  IOException e) {
    throw new RuntimeException("Failed to reset ogg stream",e);
  }
}
