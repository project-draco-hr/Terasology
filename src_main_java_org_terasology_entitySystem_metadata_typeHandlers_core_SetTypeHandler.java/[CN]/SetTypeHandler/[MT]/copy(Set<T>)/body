{
  if (value != null) {
    Set result=Sets.newHashSet();
    for (    T item : value) {
      if (item != null) {
        result.add(contentsHandler.copy(item));
      }
 else {
        result.add(null);
      }
    }
    return result;
  }
  return null;
}
