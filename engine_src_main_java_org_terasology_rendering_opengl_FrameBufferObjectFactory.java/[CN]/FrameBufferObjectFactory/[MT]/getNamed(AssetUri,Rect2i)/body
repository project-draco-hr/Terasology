{
  FrameBufferObject frameBufferObject=map.get(uri);
  if (frameBufferObject == null) {
    frameBufferObject=new FrameBufferObject(uri,region);
    map.put(uri,frameBufferObject);
  }
  return frameBufferObject;
}
