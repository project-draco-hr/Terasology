{
  setupPathManager();
  setupConfig();
  setupModuleManager();
  setupDisplay();
  setupAudio();
  setupAssetManager();
  setupBlockManager();
  setupCollisionManager();
  setupNetwork();
  setupEntitySystem();
  setupStorageManager();
  setupComponentManager();
  loadPrefabs();
}
