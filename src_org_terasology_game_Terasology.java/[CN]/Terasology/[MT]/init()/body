{
  _logger.log(Level.INFO,"Initializing Terasology...");
  Thread.currentThread().setPriority(Thread.MAX_PRIORITY);
  initLogger();
  initNativeLibs();
  initOpenAL();
  initDisplay();
  initOpenGL();
  initControls();
  initManagers();
  initTimer();
}
