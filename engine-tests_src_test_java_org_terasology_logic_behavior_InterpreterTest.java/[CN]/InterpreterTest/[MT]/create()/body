{
  node=new Node(){
    @Override public Task createTask(){
      task=spy(new Task(null){
        @Override public Status update(        float dt){
          return result;
        }
      }
);
      return task;
    }
  }
;
}
