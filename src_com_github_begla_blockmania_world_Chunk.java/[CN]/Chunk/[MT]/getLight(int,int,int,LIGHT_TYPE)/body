{
  if (Helper.getInstance().checkBounds3D(x,y,z,_sunlight)) {
    if (type == LIGHT_TYPE.SUN) {
      return _sunlight[x][y][z];
    }
 else {
      return _light[x][y][z];
    }
  }
  if (type == Chunk.LIGHT_TYPE.SUN)   return 15;
 else   return 0;
}
