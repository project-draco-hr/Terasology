{
  Vector3f vector=nextVector3f();
  while (vector.x == 0.0f && vector.y == 0.0f && vector.z == 0.0f) {
    vector=nextVector3f();
  }
  vector.normalize();
  if (size != 1.0f) {
    vector.scale(size);
  }
  return vector;
}
