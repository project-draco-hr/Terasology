{
  float x, y, z;
  do {
    x=nextFloat();
    y=nextFloat();
    z=nextFloat();
  }
 while (x == 0.0f && y == 0.0f && z == 0.0f);
  Vector3f vector=new Vector3f(x,y,z);
  vector.normalize();
  return vector;
}
