{
  if (_slider.intersects(mousePos)) {
    if (_mouseDown && _moveActive == null) {
      _moveActive=this;
    }
  }
  if (_mouseUp) {
    _moveActive=null;
    _mouseDown=false;
    _mouseUp=false;
  }
  if (_moveActive == this) {
    changeSlider(mousePos.x);
  }
}
