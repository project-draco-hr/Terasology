{
  if (_slider.intersects(mousePos)) {
    if (_mouseDown) {
      _moveActive=true;
    }
  }
  if (_mouseUp) {
    _moveActive=false;
    _mouseDown=false;
    _mouseUp=false;
  }
  if (_moveActive) {
    changeSlider(mousePos.x);
  }
}
