{
  int value=Math.round(pos / ((getSize().x - _slider.getSize().x) / (float)_range));
  value+=_min;
  if (value < _min) {
    value=_min;
  }
 else   if (value > _max) {
    value=_max;
  }
  return value;
}
