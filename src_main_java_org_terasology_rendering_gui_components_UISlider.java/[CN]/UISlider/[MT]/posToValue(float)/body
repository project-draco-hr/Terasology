{
  int value=Math.round(pos / ((getSize().x - slider.getSize().x) / (float)range));
  value+=minValue;
  if (value < minValue) {
    value=minValue;
  }
 else   if (value > maxValue) {
    value=maxValue;
  }
  return value;
}
