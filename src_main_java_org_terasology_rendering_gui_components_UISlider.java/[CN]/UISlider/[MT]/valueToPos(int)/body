{
  if (minValue < 0) {
    value+=-minValue;
  }
 else {
    value-=minValue;
  }
  float pos=value * ((getSize().x - slider.getSize().x) / (float)range);
  if (pos < 0) {
    pos=0;
  }
 else   if (pos > getSize().x) {
    pos=getSize().x;
  }
  return pos;
}
