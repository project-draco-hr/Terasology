{
  if (_min < 0) {
    value+=-_min;
  }
 else {
    value-=_min;
  }
  float pos=(int)(value * ((getSize().x - _slider.getSize().x) / (float)_range));
  if (pos < 0) {
    pos=0;
  }
 else   if (pos > getSize().x) {
    pos=getSize().x;
  }
  return pos;
}
