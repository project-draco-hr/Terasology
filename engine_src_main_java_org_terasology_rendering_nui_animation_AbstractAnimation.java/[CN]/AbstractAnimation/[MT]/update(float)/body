{
switch (this.currentState) {
case PAUSED:
case STOPPED:
    return;
case RUNNING:
{
    if (this.frames.size() == 0) {
      onEnd();
      return;
    }
    elapsedTime+=delta;
    if (elapsedTime >= frames.get(0).getDuration()) {
      frames.remove(0);
      return;
    }
    break;
  }
}
}
