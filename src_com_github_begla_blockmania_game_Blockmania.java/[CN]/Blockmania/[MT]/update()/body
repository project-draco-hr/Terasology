{
  if (!_pauseGame && !_hud.getDebugConsole().isVisible() && !_pauseMenu.isVisible()) {
    if (_world != null)     _world.update();
    Mouse.setGrabbed(true);
  }
 else {
    Mouse.setGrabbed(false);
  }
  updateUserInterface();
}
