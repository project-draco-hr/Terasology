{
  long delta=getTime() - _lastLoopTime;
  _lastLoopTime=getTime();
  _lastFpsTime+=delta;
  _fps++;
  if (_lastFpsTime >= 1000) {
    _lastFpsTime=0;
    _averageFps+=_fps;
    _averageFps/=2;
    _fps=0;
  }
}
