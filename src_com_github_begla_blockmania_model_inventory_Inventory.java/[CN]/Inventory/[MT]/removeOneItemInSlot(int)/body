{
  if (slot < 0 || slot >= size())   return null;
  if (_inventory[slot] != null) {
    Item item=_inventory[slot];
    item.decreaseAmount();
    if (item.getAmount() == 0) {
      _inventory[slot]=null;
    }
    return item;
  }
  return null;
}
