{
  for (int i=0; i < 64; i++) {
    if (_inventory[i] == null) {
      return i;
    }
 else {
      if (_inventory[i].equals(item) && _inventory[i].getAmount() < 32) {
        return i;
      }
    }
  }
  return -1;
}
