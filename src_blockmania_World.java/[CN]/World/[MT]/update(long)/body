{
  int chunkUpdates=0;
  while (chunkUpdateQueueDL.size() > 0) {
    if (chunkUpdates < 4) {
      Chunk c=chunkUpdateQueueDL.remove();
      c.generateDisplayList();
      chunkUpdates++;
    }
 else {
      break;
    }
  }
}
