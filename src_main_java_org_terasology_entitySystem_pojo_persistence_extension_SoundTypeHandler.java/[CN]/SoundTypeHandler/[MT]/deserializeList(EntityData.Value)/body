{
  List<Sound> result=Lists.newArrayListWithCapacity(value.getStringCount());
  for (  String item : value.getStringList()) {
    Sound sound=soundManager.getSound(item);
    if (sound != null)     result.add(sound);
  }
  return result;
}
