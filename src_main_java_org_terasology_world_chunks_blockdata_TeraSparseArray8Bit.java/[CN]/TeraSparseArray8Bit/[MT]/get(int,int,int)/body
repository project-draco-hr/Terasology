{
  if (!contains(x,y,z))   throw new IndexOutOfBoundsException("Index out of bounds (" + x + ", "+ y+ ", "+ z+ ")");
  if (inflated == null)   return fill;
  byte[] data=inflated[y];
  if (data != null)   return data[z * sizeX + x];
  return deflated[y];
}
