{
  logger.info("Initialising World");
  try {
    heightmap=HeightmapFileReader.readFile("Heightmap.txt","\n");
  }
 catch (  IOException e) {
    logger.error("Failed to read heightmap",e);
  }
  heightmap=shiftArray(rotateArray(heightmap),-50,-100);
}
