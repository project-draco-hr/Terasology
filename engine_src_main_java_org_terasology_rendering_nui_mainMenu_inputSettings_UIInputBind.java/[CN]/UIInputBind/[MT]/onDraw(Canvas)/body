{
  if (!capturingInput) {
    if (input.get() != null) {
      canvas.drawText(input.get().toShortString());
    }
  }
 else {
    canvas.drawText("???");
  }
  canvas.addInteractionRegion(interactionListener);
}
