{
  if (obj == this) {
    return true;
  }
  if (obj instanceof BlockUri) {
    BlockUri other=(BlockUri)obj;
    return Objects.equal(other.moduleName,moduleName) && Objects.equal(other.familyName,familyName) && Objects.equal(other.shape,shape)&& Objects.equal(other.blockIdentifier,blockIdentifier);
  }
  return false;
}
