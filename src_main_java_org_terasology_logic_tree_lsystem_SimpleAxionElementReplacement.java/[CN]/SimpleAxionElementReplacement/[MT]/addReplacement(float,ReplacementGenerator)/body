{
  if (probabilitySum + probability > 1f)   throw new IllegalArgumentException("Sum of probabilities exceeds 1");
  probabilitySum+=probability;
  probabilities.add(1 - probabilitySum);
  replacements.add(replacement);
}
