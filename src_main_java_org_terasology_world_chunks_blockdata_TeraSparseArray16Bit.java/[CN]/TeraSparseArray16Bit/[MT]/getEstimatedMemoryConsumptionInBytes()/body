{
  if (inflated == null) {
    return 10;
  }
  int result=10 + (getSizeY() * 2) + (getSizeY() * 4);
  for (int i=0; i < getSizeY(); i++) {
    if (inflated[i] != null) {
      result+=12 + (getSizeXZ() * 2);
    }
  }
  return result;
}
