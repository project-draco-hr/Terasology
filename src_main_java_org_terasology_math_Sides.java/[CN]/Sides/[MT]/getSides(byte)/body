{
  Set<Side> result=Sets.newHashSet();
  for (  Map.Entry<Side,Byte> aSideBit : sideBits.entrySet()) {
    if ((aSideBit.getValue() & sidesBit) > 0)     result.add(aSideBit.getKey());
  }
  return result;
}
