{
  if (dest == null)   dest=new Vector3i();
  int x=val[M00] * vec.x + val[M01] * vec.y + val[M02] * vec.z;
  int y=val[M10] * vec.x + val[M11] * vec.y + val[M12] * vec.z;
  int z=val[M20] * vec.x + val[M21] * vec.y + val[M22] * vec.z;
  dest.x=x;
  dest.y=y;
  dest.z=z;
  return dest;
}
