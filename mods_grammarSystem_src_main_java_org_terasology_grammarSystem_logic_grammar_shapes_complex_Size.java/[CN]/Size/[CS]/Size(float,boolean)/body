{
  this.absolute=absolute;
  if (!absolute) {
    if (value < 0 || value > 1)     throw new IllegalArgumentException("The value for relative sizes must be from the interval [0,1] but was " + value);
  }
  this.value=value;
}
