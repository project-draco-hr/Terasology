{
  float result=0.0f;
  result+=_pGen3.noiseWithOctaves(0.01f * x,0.01f,0.01f * z,8,0.5f);
  result=(float)Math.sqrt(Math.abs(result));
  result-=0.5f;
  if (result < 0f) {
    result=0;
  }
  return result * 2f;
}
