{
  if (obj == this) {
    return true;
  }
  if (obj instanceof BlockUri) {
    BlockUri other=(BlockUri)obj;
    return Objects.equal(other.normalisedModuleName,normalisedModuleName) && Objects.equal(other.normalisedFamilyName,normalisedFamilyName) && Objects.equal(other.shape,shape)&& Objects.equal(other.normalisedBlockIdentifier,normalisedBlockIdentifier);
  }
  return false;
}
